# Bundle layer applied when an alego profile lists alego-tui in its bundles
# (alego plugin --profile <p> add alego-tui). Composes over @singula-ai/alego-base.
#
# Kept deliberately small: the TUI mounts as one row and creates its own agent
# at runtime; base-bundle tool/service rows stay untouched.

# Hot reload watches source trees; pointless and noisy under a packaged TUI.
- id: hmr
  disabled: true

- insert:
    # Interactive entry points own the question tool in Alego 0.1.3.
    - id: tool-ask-user
      name: '@singula-ai/alego-tool-ask-user'

    - id: tui
      name: alego-tui
      config: {}
